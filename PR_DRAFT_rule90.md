# PR draft — approved body (adversarially vetted 2026-07-02)
# Send with: gh pr create --repo FloatingPragma/observer-patch-holography \
#   --base main --head qizwiz:feat/rule90-carrier-v2 \
#   --title "feat(LEAN): Rule 90 carrier: Hfib discharged on an information-set boundary (#304)" \
#   --body-file <this file, below the --- line>
# Status: SENT 2026-07-02 as https://github.com/FloatingPragma/observer-patch-holography/pull/385
# (pre-approved by JH conditional on green build + verification gate — gate passed, head a0ad1ed)

---
the Hfib hypothesis from #304 ("for the declared B, C_b is a singleton mod gauge") discharged on the first carrier where it's non-degenerate.. the linear case, where it's exactly erasure correction.

one Rule 90 step as a two-patch carrier: edge-consistency ⟺ the bottom row is the Rule 90 image of the seed ⟺ a valid CA diagram. rule 90 is linear over F2, so the consistent set is literally a code and "B identifies the fiber" becomes "B contains an information set".

machine-checked, standard axioms only (propext, Classical.choice, Quot.sound.. no sorry, no native_decide):

- `rule90_Hfib_good`: reading bottom cells {0,1} identifies the fiber. exactly the `Hfib` binder of `boundary_fiber_observer_unique`, instantiated.. it plugs in directly
- `rule90_Hfib_bad_fails`: reading {0,2} fails. same carrier, same dynamics, coarser read.. it reads the CA-redundant bit twice and erases the only non-redundant cell
- `rule90_gauge_nontrivial`: seeds (0,0,0) and (1,0,1) are observably identical.. a ker(Rule90) pair, the unobservable part of the seed (the exact gauge = kernel iff is next-packet material)
- `rule90_no_frustrationFree_repair`: NO local repair satisfying H1-H3 exists on this carrier at all. every Rule90 image has equal outer bits, so any target row that breaks that is out of reach: H2 forces a fire, H1 pins it to the seed, H3 demands the impossible

this is exactly the obligation from your last comment on #304: B injective on the consistent quotient C / gauge. `rule90_Hfib_good` is that statement, discharged.. and the bad-boundary theorem shows the property is about WHICH cells B reads, not how many.

the no-repair theorem is why I think your injectivity reframe is not just cleaner but forced, at least for rigid carriers like this one: the repair-selector route is provably closed here, while the injectivity route goes through. I have a sketch suggesting this generalizes ("no holography without frustration": under H1-H3 on locally-simple carriers, every Hfib boundary must carry full bulk information.. no compression) — that's the next packet if it survives.

next packet either way: the general multi-patch carrier, B injective on the consistent set ⟺ interior forced mod gauge.. the constraint-code version, where the consistent set is generated by local parity checks and "boundary determines interior" becomes literal erasure decoding.

builds green on top of b0e186e (8252 jobs, x86-64 linux). axiom audit in the build log.
